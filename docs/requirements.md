# 要件定義書 — ChatGPT「5つの魔法の合い言葉」特典ページ

## 0. アーキ構成

- 確定アーキ: #6 WEBアプリ（決定論）
- 操作者: ブラウザのエンドユーザー（Instagramリール視聴者）
- AI本体(プロンプトY): なし（ページはすべて静的。コンテンツはユーザーが事前に作成した文章をそのまま埋め込む）
- MCP: なし
- 自社DB: なし
- フロントUI: あり（このリポジトリそのものがフロントエンド全体）
- 動的変数: なし
- 種別: Web型（静的サイト。バックエンド・DB・ビルド工程なし）
- 配布: マイ専用（発信用GitHubアカウント mion-ai-mama 配下の個人リポジトリ）
- ホスティング: GitHub Pages（無料・静的ファイル配信のみ）

補足: `mion-ai-mama/instagram-tokuten-template`（テンプレートリポジトリ）から
「Use this template」で複製して作成した、1回限りの特典ページ。今後さらに複製して
使う汎用テンプレートとしての運用は想定しない（元のテンプレート自体は
`instagram-tokuten-template` 側で維持する）。

## 1. プロジェクト概要

- 成果目標: Instagramリールの視聴者に配布する特典資料を、GitHub Pages上の
  1ページ完結型LP（ランディングページ）として提供する。
- 今回の特典内容: 「ChatGPTの長文がパッとわかる！5つの魔法の合い言葉」
  （ChatGPTに5つの合い言葉を設定し、長い回答を漫画・図解・ノート・付箋・比較表の
  画像に変換させる手法を紹介）

### 成功指標（定量）

- 特典ページ → LINE登録ボタンのクリック率
- リールのプロフィールリンク経由の特典ページ到達率
- ページ平均滞在時間（最後まで読了されるか）
- 各コピーボタンの利用率（どの合い言葉・入力例がよく使われるか）

### 成功指標（定性）

- スクロールごとに「自分にもできそう」と感じさせる没入感
- スマホでサクサク読める、情報過多に感じさせない余白・構成
- 5つの合い言葉の使い分けが直感的に理解できる
- コードに詳しくない人でも `content.js` 中心の編集で運用できる

## 2. 想定読者（ペルソナ）

- 40代・50代、AI初心者、ChatGPTを使い始めたばかり
- 専門用語が苦手、スマートフォンで閲覧
- ChatGPTの長い回答を読むのが大変だと感じている

デザイン・文章とも「私にもできそう」「この通りにやればいい」と思える平易さを最優先する。

## 3. ページ構成（1ページ構成・全12セクション）

| # | セクション | 内容概要 |
|---|---|---|
| 1 | OP画像 | メインビジュアル（タイトル・サブコピーは画像内に内包。テキストでの重複表示なし） |
| 2 | 導入文 | 「長い説明、もう頑張って全部読まなくてOK！」 |
| 3 | 使い方3ステップ | 設定文コピー→貼り付け→合い言葉+テーマ入力（3カード、PC横並び・SPは縦並び） |
| 4 | 事前設定文 | ChatGPTに送る設定文＋大きいコピーボタン |
| 5 | 5つの合い言葉一覧 | /manga /visual /note /sticky /compare の説明カード |
| 6 | そのまま使える入力例 | 5つの独立したコピペ例（各カードに個別コピーボタン） |
| 7 | 自分の文章を画像にする方法 | 直前の回答を合い言葉だけで画像化する例 |
| 8 | うまくいかないときの対処法 | アコーディオン形式のFAQ（5項目、3項目に追加指示文コピーボタン） |
| 9 | 注意事項 | 淡い黄色の注意ボックス（7項目） |
| 10 | 最後のメッセージ | 事前設定文セクションへ戻るボタン |
| 11 | 最後の案内(CTA) | 『AIマネタイズの教科書』LINE登録導線（バナー画像クリック） |
| 12 | フッター | © MION、転載禁止表記 |

## 4. データ設計概要

DBは使用しない。全コンテンツは `js/content.js` の `CONTENT` オブジェクト（JavaScriptの
定数）で一元管理し、`js/script.js` がページ読み込み時にDOMへ反映する。`index.html` には
初期値として同内容を直接記述し、JavaScript無効時のフォールバック表示を担保する。

## 5. セキュリティ要件

- 個人情報の入力フォームなし、認証機能なし、決済機能なし → 該当するセキュリティ対策は最小限
- HTTPS: GitHub Pagesは標準でHTTPS配信
- 外部リンク（LINE誘導ボタン等）は `rel="noopener"` を付与
- 入力フォームが存在しないため、CSRF・入力サニタイズ等の対策は対象外

## 6. 技術スタック

- HTML5 / CSS3 / Vanilla JavaScript（フレームワーク・ビルド工程なし）
- フォント: システムフォント（追加のWebフォント読み込みなし）
- ホスティング: GitHub Pages（`main` ブランチ / root）
- ビルドツール・パッケージマネージャ: 使用しない
- データベース・サーバーサイド処理: 使用しない

## 7. 外部サービス一覧

| サービス | 用途 | 費用 |
|---|---|---|
| GitHub Pages | 静的サイトホスティング | 無料 |
| LINE公式アカウント | 特典受け取り後の登録導線（既存の導線をそのまま利用） | 既存のため対象外 |

## 8. AI設計

該当なし（AI本体Yなし）。ページ自体にAIのランタイム処理は含まれない。

## 9. OGP・CTAに関する方針（本ページ固有の決定）

- **OGP画像**: このシリーズの標準方針（OGP画像は設定しない）から今回は例外的に変更し、
  OP画像（`assets/images/chatgpt-keywords-hero.png`）をOGP画像として設定する
  （`og:image` / `twitter:card: summary_large_image`）。以降のページもOGP画像を
  設定するかどうかはページごとに判断する運用に変更した（2026-09-03、ユーザー承認）。
- **CTA**: シリーズ標準どおり『AIマネタイズの教科書』LINE登録CTAを常時表示する
  （バナー画像・リンク先は既存と同一、橋渡し文のみ本ページの内容に合わせて書き直し）。
